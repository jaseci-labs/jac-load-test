"""Workspace CRUD walkers.

Phase 6 walkers to implement:
  - create_workspace(name, mode, target_url?, services_map_json?,
                     credential_mode, username?, password?,
                     login_path, include_static) -> Workspace
  - list_workspaces()              -> list[Workspace]
  - get_workspace(id)              -> Workspace
  - update_workspace(id, ...)      -> Workspace
  - delete_workspace(id)           -> {ok}
"""
